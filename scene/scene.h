/** Any new scenes should be included here.
 * 
 *  enum class Scene in globals.h will also need to be changed
 *  in order to switch over them in game.cpp (render and update)
 * */
#include "menu.h"
#include "default.h"
#include "lvl1.h"

#include "../globals.h"

namespace scene{
}